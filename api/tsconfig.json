{
    "compilerOptions": {
      "target": "ES6",                    /* Versión de ECMAScript a la que compilar (puedes cambiarla según tus necesidades) */
      "module": "CommonJS",               /* Sistema de módulos a utilizar (por ejemplo, CommonJS, ESNext) */
      "outDir": "./dist",                 /* Directorio de salida para los archivos compilados */
      "rootDir": "./src",                 /* Directorio raíz de los archivos fuente TypeScript */
      "strict": true,                     /* Habilitar comprobaciones estrictas de tipo */
      "esModuleInterop": true,            /* Habilitar interoperabilidad de módulos ES6 */
      "forceConsistentCasingInFileNames": true, /* Forzar consistencia en mayúsculas y minúsculas en nombres de archivos */
      "noImplicitAny": true,              /* Mostrar errores si se usa 'any' implícitamente */
      "strictNullChecks": true,           /* Habilitar comprobaciones estrictas de nulos */
      "strictFunctionTypes": true,        /* Comprobaciones estrictas en tipos de funciones */
      "strictPropertyInitialization": true /* Comprobaciones estrictas en inicialización de propiedades */
    },
    "include": ["src/**/*.ts"],           /* Patrón de inclusión de archivos TypeScript */
    "exclude": ["node_modules"]           /* Patrón de exclusión de archivos o directorios */
  }